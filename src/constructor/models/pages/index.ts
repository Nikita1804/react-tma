import { types as t } from 'mobx-state-tree';

export const PagesModel = t.model('PagesModel', {
  // categoriesModel: t.optional(CategoriesModel, {}),
  // regionsModel: t.optional(RegionsModel, {}),
  // detailProductModel: t.optional(DetailProductModel, {}),
  // loginModel: t.optional(LoginModel, {}),
  // singInModel: t.optional(SingInModel, {}),
});
