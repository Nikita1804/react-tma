import { types as t } from 'mobx-state-tree';

export const Controllers = t.model('Controllers', {
  // categoriesController: t.optional(CategoriesController, {}),
  // regionsController: t.optional(RegionsController, {}),
  // detailProductController: t.optional(DetailProductController, {}),
});
